CREATE TABLE IF NOT EXISTS analysis_drafts (user_id INTEGER PRIMARY KEY, payload_json TEXT NOT NULL, current_step INTEGER NOT NULL DEFAULT 1, schema_version TEXT NOT NULL DEFAULT '1', updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);
